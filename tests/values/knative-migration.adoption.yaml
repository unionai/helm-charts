# Helm-install path: exercises adoption.enabled=true. Verifies that the
# extra RBAC rule renders, the env vars are wired, and the cleanup.sh
# adoption step is included.
adoption:
  enabled: true
  targetRelease: unionai-dataplane
  targetNamespace: union
