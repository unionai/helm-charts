# helm-values: examples/values-test-certs.yaml
#
# low_privilege: true AND namespaces.enabled: true. low_privilege must still
# force namespaced RBAC and suppress namespace pre-seeding. The opposite
# direction is covered by dataplane.namespace-multi.yaml.
host: union.us-west-2.union.ai
clusterName: my-cluster
orgName: union
provider: metal
low_privilege: true
namespaces:
  enabled: true
storage:
  provider: "compat"
  endpoint: http://localstack.aws.svc.cluster.local:4566
  accessKey: test123
  secretKey: test
  bucketName: bucket
  fastRegistrationBucketName: bucket
  region: us-west-2
imageBuilder:
  defaultRepository: "localhost:5000/union-dataplane"
secrets:
  admin:
    create: true
    clientId: my-client-id
    clientSecret: my-secret
