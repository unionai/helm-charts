# helm-values: examples/values-test-certs.yaml
# helm-args: --is-upgrade

image:
  kubectl:
    repository: harbor.example.com/dockerhub/bitnami/kubectl
    tag: "1.32.3"
    pullPolicy: IfNotPresent
    imagePullSecrets:
      - name: harbor-pull-secret

secrets:
  admin:
    create: false
