# knative-operator-crds (DEPRECATED)

> **Deprecated.** Knative Operator + Serving CRDs are now vendored at
> [`crds/knative-operator/`](../../crds/knative-operator/) — installed via
> ArgoCD's `directory:` source with the
> `argocd.argoproj.io/sync-options: ServerSideApply=true` annotation
> injected per CRD. Required for App Serving on selfhosted deployments.
>
> This chart is retained for back-compat while consumers migrate. Will be
> removed in a future release.
