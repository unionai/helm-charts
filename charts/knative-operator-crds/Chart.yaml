apiVersion: v2
name: knative-operator-crds
description: |
  DEPRECATED. Knative Operator + Serving CRDs are now vendored at
  `crds/knative-operator/`. This chart is retained for back-compat while
  consumers migrate. Will be removed in a future release.
deprecated: true
type: application
version: 2026.4.5
appVersion: 1.16.0
kubeVersion: ">= 1.28.0-0"
