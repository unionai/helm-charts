{{- if .Values.flytepropellerwebhook.enabled }}
{{- if and (not .Values.flytepropellerwebhook.managedConfig) .Values.low_privilege }}
{{- fail "flytepropellerwebhook.managedConfig: false requires low_privilege: false. With managedConfig false the webhook registers its own MutatingWebhookConfiguration, a cluster-scoped resource that no namespaced Role can grant, and without that registration the webhook is sent no pods at all. Set flytepropellerwebhook.managedConfig: true to have Helm create the configuration instead, or set low_privilege: false." }}
{{- end }}
{{/*
The webhook mirrors image-pull secrets into work namespaces, which needs a
RoleBinding for union-work-ns. The Forbidden is never surfaced: the pod is
admitted without its secret and later hits ImagePullBackOff. Fail the render.
*/}}
{{- $reach := or
      (include "dataplane.rbac.staticPosture" .)
      (and .Values.clusterresourcesync.enabled (not (include "singleNamespace" .)))
      .Values.rbac.externalBindingProvisioner }}
{{- if and (not (include "singleNamespace" .)) (not $reach) .Values.config.core.webhook.embeddedSecretManagerConfig.imagePullSecrets.enabled }}
{{- fail "config.core.webhook.embeddedSecretManagerConfig.imagePullSecrets.enabled is true, but nothing in this configuration gives flytepropellerwebhook write access to the work namespaces. The webhook copies image-pull secrets into each work namespace, and the Forbidden is not reported: the pod is admitted without its secret and later fails with ImagePullBackOff. Union components get access to a work namespace only through a RoleBinding for the union-work-ns ClusterRole, so create one by one of: namespaces.enabled: true with a non-empty namespaces.static, and this chart creates the bindings; or clusterresourcesync.enabled: true, and it creates them for each namespace it provisions; or rbac.externalBindingProvisioner: true if your own tooling creates them. Or set config.core.webhook.embeddedSecretManagerConfig.imagePullSecrets.enabled: false if this deployment does not use managed images." }}
{{- end }}
{{- if not (include "useCommonServiceAccount" .) }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "webhook.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  {{- with include "global.serviceAccountAnnotations" . }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
{{- with .Values.flytepropellerwebhook.serviceAccount.imagePullSecrets }}
imagePullSecrets: {{ tpl (toYaml .) $ | nindent 2 }}
{{- end }}
{{- end }}
{{- end }}
{{- define "dataplane.rbac.slots.webhook" -}}
{{/*
No components-namespace grant: the serving cert is mounted as a volume whichever
provider creates it. The secrets writes here are image-pull-secret injection.
*/}}
work-ns:
  - apiGroups: ['*']
    resources: [secrets, pods, replicasets/finalizers]
{{- if .Values.config.core.webhook.embeddedSecretManagerConfig.imagePullSecrets.enabled }}
{{/*
Cluster-wide Secret read that cannot be narrowed: the informer cache is limited
only by DefaultNamespaces, set when limitNamespace is neither empty nor "all"
(flyte/flytepropeller/pkg/secret/secrets_injector.go:75-83), which this chart does
only under singleNamespace. Narrowing it would break mirroring anyway, since
embedded_secret_manager.go:263 reads the mirrored secret from the admitted pod's
namespace through the same cache. Without the grant the webhook hangs in
WaitForCacheSync and failurePolicy: Fail rejects every pod labelled
inject-flyte-secrets.
*/}}
work-ns-cluster-read:
  - apiGroups: [""]
    resources: [secrets]
    verbs: [list, watch]
{{- end }}
{{- if not .Values.flytepropellerwebhook.managedConfig }}
{{/*
Only emitted at low_privilege: false; the guard at the top of this file fails the
render otherwise. Verbs are listed explicitly to leave out delete.
*/}}
cluster-write:
  - apiGroups: [admissionregistration.k8s.io]
    resources: [mutatingwebhookconfigurations]
    verbs: [get, create, update, patch]
{{- end }}
{{- end -}}
