apiVersion: v2
name: envoy-gateway-config
description: Cluster-operator resources for Envoy Gateway — GatewayClass, EnvoyProxy, BackendTrafficPolicy, and EnvoyPatchPolicy.
type: application
version: 0.1.0
kubeVersion: '>= 1.28.0-0'
