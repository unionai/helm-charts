{{- if .Values.dgxagent.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "dgxagent.name" . }}
  labels:
    {{- include "dgxagent.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "dgxagent.name" . }}
  minReplicas: {{ .Values.dgxagent.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.dgxagent.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.dgxagent.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.dgxagent.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.dgxagent.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.dgxagent.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
