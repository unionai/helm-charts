{{- if .Values.flyteagent.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "flyteagent.name" . }}
  labels:
    {{- include "flyteagent.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "flyteagent.name" . }}
  minReplicas: {{ .Values.flyteagent.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.flyteagent.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.flyteagent.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.flyteagent.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.flyteagent.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.flyteagent.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
