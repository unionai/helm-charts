{{- if .Values.scylla.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: scylla-config
  namespace: {{ .Release.Namespace }}
data:
  scylla.yaml: |
{{- if not .Values.scylla.developerMode }}
    authenticator: PasswordAuthenticator
    authorizer: CassandraAuthorizer
{{- end }}
{{- end }}
