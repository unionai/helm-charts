# ============================================================================
# DEPRECATED — historical reference only.
# ============================================================================
# This file was the previous canonical `charts/controlplane/values.gcp.yaml`
# before the FAB-272 consolidation (chart release 2026.X.Y) — a monitoring-only
# stub for GKE-specific scrape exclusions.
#
# DO NOT start a new deployment from this file. The chart does not test it,
# the chart will not maintain it, and behavior may drift silently from the
# canonical `../values.gcp.yaml` as the chart evolves. The canonical overlay
# already includes these GKE monitoring exclusions.
#
# All deployment modes (selfmanaged, selfhosted, BYOC) use `../values.gcp.yaml`
# as the base overlay. There is no chart-level distinction between modes;
# env-specific globals are supplied via your own overlay layered on top.
#
# See `charts/MIGRATION.md` for the migration story.
# ============================================================================

# ----------------------------------------------------------------------------
# Monitoring Configuration (GCP/GKE specific)
# ----------------------------------------------------------------------------

# GKE manages controller-manager, scheduler, etcd, and kube-proxy internally.
# Their metrics endpoints are not accessible from within the cluster.
# Disable scraping and alerting rules for these components to avoid
# false-positive TargetDown and *Down alerts. Mirrors values.aws.yaml.
monitoring:
  kubeControllerManager:
    enabled: false
  kubeScheduler:
    enabled: false
  kubeEtcd:
    enabled: false
  kubeProxy:
    enabled: false
  defaultRules:
    rules:
      kubeControllerManager: false
      kubeSchedulerAlerting: false
      kubeSchedulerRecording: false
      kubeProxy: false
      etcd: false
