# ============================================================================
# Example: Union Control Plane GCP monitoring stub (legacy, not tested)
# ============================================================================
# This file was the previous content of `charts/controlplane/values.gcp.yaml` —
# a monitoring-only stub for GKE-specific scrape exclusions. Preserved here
# for reference. NOT covered by snapshot tests.
#
# For supported, actively-tested GCP deployments use `../values.gcp.yaml`
# (the canonical overlay includes these GKE monitoring exclusions already).
# See `charts/MIGRATION.md`.
# ============================================================================

# ----------------------------------------------------------------------------
# Monitoring Configuration (GCP/GKE specific)
# ----------------------------------------------------------------------------

# GKE manages controller-manager, scheduler, etcd, and kube-proxy internally.
# Their metrics endpoints are not accessible from within the cluster.
# Disable scraping and alerting rules for these components to avoid
# false-positive TargetDown and *Down alerts. Mirrors values.aws.yaml.
monitoring:
  kubeControllerManager:
    enabled: false
  kubeScheduler:
    enabled: false
  kubeEtcd:
    enabled: false
  kubeProxy:
    enabled: false
  defaultRules:
    rules:
      kubeControllerManager: false
      kubeSchedulerAlerting: false
      kubeSchedulerRecording: false
      kubeProxy: false
      etcd: false
