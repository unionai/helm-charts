repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.4.0
    hooks:
      # Enable later
      # - id: check-yaml
      # tests/generated/ records `helm template` output verbatim and is
      # byte-compared by `make test`, so formatters must leave it alone. Helm
      # ends its output with a blank line; stripping it fails the diff.
      - id: end-of-file-fixer
        exclude: tests/generated/*
      - id: trailing-whitespace
        exclude: tests/generated/*
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.1
    hooks:
      - id: ruff-check
        args: [--fix]
      - id: ruff-format
  - repo: local
    hooks:
      - id: check-chart-version-bump
        name: Ensure Chart.yaml version is bumped with appVersion
        language: script
        entry: scripts/check-chart-version-bump.sh
        files: Chart\.yaml$
